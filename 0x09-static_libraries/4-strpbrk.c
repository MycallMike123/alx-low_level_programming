#include "main.h"

/**
 * _strpbrk - Searches a string of a set of bytes
 * @s: Pointer to the string to search
 * @accept: Pointer to the string searched
 * Return: A pointer to first occurrence of s in @accept or NULL  otherwise
 */

char *_strpbrk(char *s, char *accept)
{
}
