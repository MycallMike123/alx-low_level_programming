#include "main.h"

/**
 * _new-name - prints its name
 * @argc: number of command line arguments
 * @argv: An array of strings containing the command-line
 * Return: 0 success
 */

int _new-name(int argc, char *argv[])
{
	_putchar(argv[0]);
	_putchar('\n');

	return (0);
}
